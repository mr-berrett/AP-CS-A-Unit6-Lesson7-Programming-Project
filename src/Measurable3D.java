// implementing this interface indicates the class represents a 3D
// object that has both measurable volume and surface area
public interface Measurable3D {
    public double getSurfaceArea();
    public double getVolume();
}
